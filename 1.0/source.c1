{
    int t;
    input(t);

    if (t > 10) output(-1);
    else {
        int x;
        x = 1;
        while (t > 0) {
            x = 5 + x * t;
            t = t - 5;
        }
        output(x);
    }
}